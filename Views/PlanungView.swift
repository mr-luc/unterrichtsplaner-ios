import SwiftUI

struct PlanungView: View {
    var body: some View {
        NavigationStack {
            Text("Stundenplanung kommt hier hin")
                .navigationTitle("Planung")
        }
    }
}
