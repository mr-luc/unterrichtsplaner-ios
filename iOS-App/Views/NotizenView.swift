import SwiftUI

struct NotizenView: View {
    var body: some View {
        NavigationStack {
            Text("Notizen kommen hier hin")
                .navigationTitle("Notizen")
        }
    }
}
